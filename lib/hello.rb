# Default is "World"
# Author: Jim Palowski (jim@somewhere.com)
name = ARGV.first || "World"

puts "Hello, #{name}!"

last = ARGV.first || "Hi"

puts "Hello, #{name}!"

first = ARGV.first || "Hi"

puts "Hello, #{name}!"

middle = ARGV.first || "Hi"

puts "Hello, #{name}!"